server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AllanSagastegui/library
          default-label: main
          search-paths: config-server/config